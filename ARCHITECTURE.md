# 壁纸延展 - 技术架构文档

## 一、iOS 17 Extend Wallpaper 效果拆解

iOS 17 的 Extend Wallpaper 工作原理：
- 当图片比例与屏幕比例不一致时（通常是竖图高度不够）
- 系统自动将图片**居中**，上方/下方留出的空白区域
- 用图片**顶部/底部边缘的模糊版本**填充空白
- 模糊区与清晰主体区之间做**渐变羽化**，让过渡自然

## 二、核心算法流程

```
┌─────────────┐
│  原始图片     │
│  1080×1350   │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│  Step 1: Cover 缩放          │
│  等比缩放，裁掉多余部分       │
│  1080×1350 → 1080×2400       │
│  （居中裁剪，保留核心区域）    │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Step 2: 检测延展方向         │
│  高度 < 目标高度 → 上下延展    │
│  宽度 < 目标宽度 → 左右延展    │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Step 3: 边缘条带采样         │
│  取原图顶部 20% 条带          │
│  取原图底部 20% 条带          │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Step 4: 高斯模糊             │
│  Stack Blur 算法             │
│  半径可调 (1-60px)            │
│  先降采样再模糊，性能优先      │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Step 5: 拉伸填充             │
│  模糊条带拉伸到延展区尺寸      │
│  顶部模糊区 + 主体 + 底部模糊区 │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Step 6: 羽化过渡             │
│  主体顶部 → 渐变透明           │
│  主体底部 → 渐变透明           │
│  让模糊区和清晰区自然融合      │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────┐
│  最终壁纸     │
│  1080×2400   │
└─────────────┘
```

## 三、关键代码说明

### WallpaperProcessor.kt

核心方法：

| 方法 | 作用 |
|------|------|
| `process()` | 入口，协调整个处理流程 |
| `calculateCoverRect()` | 计算 Cover 缩放矩形 |
| `extendVertically()` | 上下延展（最常见场景） |
| `extendHorizontally()` | 左右延展 |
| `drawExtendedBand()` | 绘制单条延展带（采样→模糊→拉伸） |
| `applyEdgeFeathering()` | 羽化过渡 |
| `fastBlur()` | 快速模糊（降采样 + Stack Blur） |
| `stackBlur()` | Stack Blur 算法实现 |

### 模糊算法选择

| 方案 | 最低 API | 性能 | 适用场景 |
|------|---------|------|---------|
| **Stack Blur (当前)** | API 1+ | ⭐⭐⭐ | 离线处理，兼容性好 |
| RenderEffect | API 31+ | ⭐⭐⭐⭐⭐ | 实时预览（后续优化） |
| RenderScript | API 17+ | ⭐⭐⭐⭐ | 已废弃，不推荐 |

> 当前使用 Stack Blur 保证兼容性（minSdk 24）。后续可加 RenderEffect 分支做实时预览。

## 四、扩展方向

### Phase 2: 实时预览优化
- 用 RenderEffect 做实时模糊（Android 12+）
- 低版本继续用 Stack Blur

### Phase 3: Depth Effect（主体分离）
```
原图 → ML 分割（U2Net / MediaPipe）→ 主体蒙版
→ 背景延展模糊 + 主体保持清晰
→ 时钟区域主体在前、背景在后
```

### Phase 4: LSPosed 系统级
- Hook WallpaperManager 自动预处理
- 锁屏背景绘制替换

### Phase 5: 更多效果
- [ ] 渐变延展（取主色做线性渐变）
- [ ] 镜像延展（边缘像素镜像反射）
- [ ] AI 扩图（Stable Diffusion inpainting）
- [ ] 批量处理
- [ ] 壁纸管理（历史记录、收藏）

## 五、性能优化要点

1. **降采样**：加载时 inSampleSize，处理时限制最大 2048px
2. **模糊降采样**：大图先缩放到 50% 再模糊，最后放大
3. **防抖**：SeekBar 滑动延迟 300ms 执行
4. **协程**：处理放在 Dispatchers.Default，不阻塞 UI
5. **Bitmap 复用**：及时 recycle，避免内存泄漏
6. **后续优化**：Android 12+ 用 RenderEffect 硬件加速

## 六、常见问题

**Q: 延展区看起来很假？**
A: 增大羽化宽度（blendFeather），或减小延展比例。模糊区域越大越容易看出破绽。

**Q: 处理很慢？**
A: 确保图片不超过 2048px。Android 12+ 建议用 RenderEffect。

**Q: 设置壁纸失败？**
A: 检查是否有 SET_WALLPAPER 权限。部分国产 ROM 需要在设置里手动授权。
